double myPow(double x, int n) {
    double ans;
    if(n>0){
        ans=pow(x,n);
    }
    else if(n<0){
        ans=pow(x,n);
        
    }
    else if(n==0){
        ans=pow(x,n);
    }
    return ans;
}
